name: Trigger auto deployment for container-app-okgvzcrru7dw6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-okgvzcrru7dw6-AutoDeployTrigger-09bb595e-4ad0-4657-a95e-0171b8735586.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPOKGVZCRRU7DW6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPOKGVZCRRU7DW6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPOKGVZCRRU7DW6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryokgvzcrru7dw6.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPOKGVZCRRU7DW6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPOKGVZCRRU7DW6_REGISTRY_PASSWORD }}
          containerAppName: container-app-okgvzcrru7dw6
          resourceGroup: ghatestb049
          imageToBuild: containerregistryokgvzcrru7dw6.azurecr.io/container-app-okgvzcrru7dw6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


